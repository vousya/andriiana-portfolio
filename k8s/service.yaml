apiVersion: v1
kind: Service
metadata:
  name: portfolio
  namespace: portfolio
  labels:
    app.kubernetes.io/name: andriiana-portfolio
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: andriiana-portfolio
  ports:
    - name: http
      port: 80
      targetPort: http
